[general]
	[general.id_column]
		value = 'newID'
		dtype = 'integer'  
	[general.target]
		value = './data/records_parquet/macho'
		dtype = 'string' 

[context_features]
	value = ['label', 'length', 'id']
	dtypes = ['integer', 'integer', 'string']

[sequential_features]
	value = ['dim_0', 'dim_1', 'dim_2']
	dtypes = ['float', 'float', 'float']