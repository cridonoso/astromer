version: '3'
services:
    model:
        build: .
        container_name: astromer
        volumes:
            - ./:/tf/astromer/
        ports:
            - 6006:6006
            - 8889:8888
        # runtime: nvidia
        environment:
            FLASK_DEBUG: 1
